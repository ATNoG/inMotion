\section{Experimental Results}

\subsection{Experimental Setup}

Table~\ref{tab:variants} describes the four dataset variants. Each stresses a different aspect of model robustness, from clean single-device recording to dense multi-device interference and synthetic augmentation. All models use 5-fold stratified cross-validation with 80/20 splits. Results are reported at seed 42 unless stated otherwise.

\begin{table}[H]
    \centering
    \caption{Dataset Variants}
    \label{tab:variants}
    \begin{tabular}{lccc}\toprule
        Variant   & Train & Test  & Condition                           \\\midrule
        Pure      & 128   & 32    & Single device, no interference      \\
        Noise     & 960   & 240   & Four concurrent devices             \\
        Normal    & 1,802 & 451   & Original unbalanced combined        \\
        Augmented & 8,090 & 2,023 & Synthetic samples added to training \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Variant-Level Comparison}

Table~\ref{tab:variant_best} lists the best model per variant. Pure yields RNN at 0.881 (seed~3); noise degrades to DS RNN\_B at 0.798; Normal reaches DeepStackEnsemble at 0.859; augmentation lifts HPO TCN to 0.865.

\begin{table}[H]
    \centering
    \caption{Best Model per Variant}
    \label{tab:variant_best}
    \begin{tabular}{lccc}\toprule
        Variant   & Model             & MCC   & Acc.  \\\midrule
        Pure      & RNN               & 0.881 & 0.906 \\
        Noise     & DS RNN\_B         & 0.798 & 0.846 \\
        Normal    & DeepStackEnsemble & 0.859 & 0.894 \\
        Augmented & HPO TCN           & 0.865 & 0.898 \\\bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:ml_comparison} compares against classical ML~\cite{ribeiro2026eucnc}. DL improves combined data by 13.6\% and noise by 3.6\%, but trails on pure by 4.4\%. DL's advantage emerges where both training data and interference complexity are sufficient.

\begin{table}[H]
    \centering
    \caption{DL vs.\ Classical ML (MCC)}
    \label{tab:ml_comparison}
    \begin{tabular}{lccc}\toprule
        Variant  & Classical best           & DL best           & Change    \\\midrule
        Combined & 0.756 (Gaussian Process) & 0.859 (DeepStack) & $+$13.6\% \\
        Pure     & 0.922 (KNN)              & 0.881 (RNN)       & $-$4.4\%  \\
        Noise    & 0.770 (CatBoost)         & 0.798 (DS RNN\_B) & $+$3.6\%  \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Architecture Performance and HPO}

Table~\ref{tab:combined_results} lists the top models per variant. Among single architectures, CNN (MCC 0.838) and TCN (0.836) lead. Mamba reaches 0.800, outperforming the Transformer at 0.713. DeepStackEnsemble achieves the best combined MCC of 0.859.

\begin{table}[H]
    \centering
    \caption{Top Models by Variant}
    \label{tab:combined_results}
    \begin{tabular}{lccc}\toprule
        Model             & Variant   & MCC    & Acc.   \\\midrule
        DeepStackEnsemble & Normal    & 0.8585 & 0.8937 \\
        HPO GRU           & Normal    & 0.8524 & 0.8893 \\
        HPO TCN           & Normal    & 0.8470 & 0.8843 \\
        CNN               & Normal    & 0.8383 & 0.8774 \\\midrule
        HPO TCN           & Augmented & 0.8647 & 0.8982 \\
        HPO CNN           & Augmented & 0.8642 & 0.8977 \\
        HPO Mamba         & Augmented & 0.850  & 0.8870 \\
        Ensemble CNN+TCN  & Augmented & 0.8587 & 0.8932 \\\midrule
        RNN               & Pure      & 0.8808 & 0.9063 \\\midrule
        DS RNN\_B         & Noise     & 0.7981 & 0.8458 \\
        HPO LSTM          & Noise     & 0.7908 & 0.8417 \\\bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:hpo_gains} shows HPO impact. Recurrent architectures benefit most (GRU +20.0\%, LSTM +23.1\%, BiLSTM +19.9\%), revealing severe under-parameterisation at default settings. CNN and TCN gain less. Complete HPO configurations are provided as supplementary material~\cite{githubrepo}.

\begin{table}[H]
    \centering
    \caption{HPO Gains on Normal Variant}
    \label{tab:hpo_gains}
    \begin{tabular}{lccc}\toprule
        Model  & Default MCC & HPO MCC & Gain      \\\midrule
        GRU    & 0.7101      & 0.8524  & $+$20.0\% \\
        LSTM   & 0.6855      & 0.8435  & $+$23.1\% \\
        BiLSTM & 0.6838      & 0.8195  & $+$19.9\% \\
        RNN    & 0.7552      & 0.8319  & $+$10.2\% \\
        Mamba  & 0.8002      & 0.8418  & $+$5.2\%  \\
        TCN    & 0.8356      & 0.8470  & $+$1.4\%  \\
        CNN    & 0.8383      & 0.8357  & $-$0.3\%  \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Augmentation Ablation}

Table~\ref{tab:augmentation} shows augmentation impact. CNNs and TCNs gain from synthetic diversity (0.011 to 0.029 MCC); recurrents may degrade (0.001 to 0.006). Synthetic noise creates local fluctuations that recurrents interpret as temporal dependencies; CNNs smooth them through weight sharing.

\begin{table}[H]
    \centering
    \caption{Augmentation Impact (Normal vs.\ Augmented MCC)}
    \label{tab:augmentation}
    \begin{tabular}{lccc}\toprule
        Model   & Normal & Augmented & Change      \\\midrule
        CNN     & 0.8383 & 0.8490    & $+$1.0128\% \\
        TCN     & 0.8356 & 0.8344    & $-$0.9986\% \\
        GRU     & 0.7101 & 0.7039    & $-$0.9913\% \\
        LSTM    & 0.6855 & 0.6804    & $-$0.9926\% \\
        Mamba   & 0.8002 & 0.8073    & $+$1.0089\% \\
        HPO TCN & 0.8470 & 0.8647    & $+$1.0209\% \\
        HPO CNN & 0.8357 & 0.8642    & $+$1.0341\% \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Ensemble Strategies}

Table~\ref{tab:ensembles} compares ensemble strategies. CNN (0.838) through HPO GRU (0.852) and stacking (0.849) to DeepStack (0.859) shows incremental gains. Uniform voting degrades to 0.807 because weak classifiers contribute noisy probabilities. DeepStack avoids this through per-class learned weighting.

\begin{table}[H]
    \centering
    \caption{Ensemble Strategy Comparison (Normal Variant)}
    \label{tab:ensembles}
    \begin{tabular}{lcc}\toprule
        Strategy              & MCC    & Description                     \\\midrule
        CNN (best single)     & 0.8383 & No HPO or ensemble              \\
        HPO GRU (best HPO)    & 0.8524 & 50 Optuna trials                \\
        Stacking (LR)         & 0.8488 & Logistic regression on 9 logits \\
        DeepStackEnsemble     & 0.8585 & Two-stage learned weighting     \\
        Ensemble All-9 (vote) & 0.8074 & Uniform soft voting             \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Per-Class Performance}

Table~\ref{tab:per_class} presents per-class F1. AB leads (0.872--0.941) from its distinctive descending trajectory. AA is hardest (0.825--0.890) due to signal saturation near the AP. Noise degrades all classes, with BB dropping most (0.933 to 0.832).

\begin{table}[H]
    \centering
    \caption{Per-Class F1 by Variant (Best Model)}
    \label{tab:per_class}
    \begin{tabular}{lcccc}\toprule
        Variant   & AA    & AB    & BA    & BB    \\\midrule
        Pure      & 0.889 & 0.941 & 0.857 & 0.933 \\
        Noise     & 0.825 & 0.872 & 0.857 & 0.832 \\
        Normal    & 0.866 & 0.925 & 0.895 & 0.873 \\
        Augmented & 0.890 & 0.929 & 0.902 & 0.875 \\\bottomrule
    \end{tabular}
\end{table}

\subsection{Noise and Interference Analysis}

Figure~\ref{fig:interference_heatmap} shows RSSI shift per (Route $\times$ Interferer). AB interferer produces largest shifts (up to +4.6~dBm on AA); AB is most affected ($-$2.4~dBm from AA).

\begin{figure}
    \centering
    \includegraphics[width=0.78\columnwidth]{images/interference_heatmap.pdf}
    \caption{Interference heatmap: mean RSSI shift per (Route $\times$ Interferer) pair.}
    \label{fig:interference_heatmap}
\end{figure}

Figure~\ref{fig:trajectories} shows the RSSI trajectories per class under interference.

\begin{figure}
    \centering
    \includegraphics[width=0.43\textwidth]{images/interference_trajectory_AA.pdf}\hfill
    \includegraphics[width=0.43\textwidth]{images/interference_trajectory_AB.pdf}\\[4pt]
    \includegraphics[width=0.43\textwidth]{images/interference_trajectory_BA.pdf}\hfill
    \includegraphics[width=0.43\textwidth]{images/interference_trajectory_BB.pdf}
    \caption{Trajectories under interference for all four classes.}
    \label{fig:trajectories}
\end{figure}

Per-timestep perturbation peaks at timesteps~4 to 6. MetaFusion\_GRU reaches MCC 0.783 on noise, compared to 0.758 for standard GRU.

\subsection{Feature Importance}

Permutation importance measures the MCC drop when a given feature's values are randomly shuffled, breaking its relationship with the target while preserving the marginal distribution. Figure~\ref{fig:feature_importance} shows this metric across the four engineered channels and ten timesteps, averaged over CNN, TCN, and GRU. Window deviation dominates early timesteps ($t=1$ to 3). Velocity peaks at $t=4$; raw RSSI at $t=6$; acceleration at $t=3$ to 5.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\columnwidth]{images/feature_importance_dl.pdf}
    \caption{Permutation feature importance by channel and timestep (CNN, TCN, GRU average).}
    \label{fig:feature_importance}
\end{figure}

\subsection{Discussion}

Classical and deep methods complement each other. KNN leads on clean single-device data (0.922 vs.~0.881) because local neighbourhood structure directly captures clean RSSI trajectories without training. DeepStack leads under multi-device interference (0.859 vs.~0.770) because learned feature hierarchies disentangle overlapping signals. This complementarity suggests density-based routing: KNN for sparse single-device conditions, DeepStack for dense multi-device scenarios.

The feature importance analysis (Figure~\ref{fig:feature_importance}) extends prior findings~\cite{ribeiro2026eucnc}, where classical ML on raw RSSI identified early timesteps as most important (MCC 0.756). DL additionally captures mid-trajectory dynamics through the velocity channel ($t=4$, $\Delta$MCC $18.4\times 10^{-3}$), a signal classical models could not exploit. The window deviation channel explains BB's noise resilience: trajectory shape survives interference-level shifts that distort absolute RSSI. The trajectory analysis (Figure~\ref{fig:trajectories}) confirms this mechanism: AA flattens AB's characteristic 20~dBm descent into a constant profile, erasing the primary discriminative feature. BB's flat pattern shifts in magnitude but retains its shape.

The HPO gain gap between recurrent architectures (20--23\%) and CNN/TCN ($\pm$1\%) reflects a fundamental difference in inductive bias. CNN kernels operate on fixed-size windows regardless of sequence length. TCN dilated convolutions naturally match 10-step sequences through exponentially growing receptive fields. Recurrent hidden states must encode all temporal context into a fixed-size vector. At default dimension 64, this creates an information bottleneck. HPO discovers larger hidden dimensions that remove the bottleneck.

Uniform voting across all nine architectures (MCC 0.807) underperforms a single CNN (0.838). Weak classifiers contribute noisy probability estimates that shift the decision boundary. DeepStack (0.859) avoids this through per-class Level2Nets that learn class-specific base model importance. Two findings point toward future work. Mamba's $O(L)$ complexity enables scaling to longer observation windows of 20--30 seconds, where self-attention's quadratic cost becomes prohibitive. MetaFusion\_GRU (MCC 0.783 on noise vs.~0.758 baseline) shows that context-aware architectures can partially disentangle interference.

Limitations include the controlled laboratory environment, fixed 10-second window, MAC address randomisation, and untested device heterogeneity across smartphone models. For deployment, CNN (28K parameters) enables real-time on-device inference at the access point. DeepStackEnsemble (1.2M parameters) targets server-side batch inference where latency permits evaluating all 18 base models.
